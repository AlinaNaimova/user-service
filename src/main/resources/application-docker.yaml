spring:
  config:
    activate:
      on-profile: docker
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://postgres:5432/userdb
    username: user
    password: password
  data:
    redis:
      host: redis
      port: 6379
  liquibase:
    change-log: classpath:db/changelog/schema-changelog.yaml

server:
  port: 8080

jwt:
  secret: "mySuperSecretKeyThatIsAtLeast32BytesLongForJWT123!"

app:
  user-service:
    url: "http://user-service:8080/api/users"

logging:
  level:
    com.example.userservice: INFO

